# Water Station (Monorepo)

This repository is organized to keep the **backend** and **frontend** separate.

## Structure

- `backend/` — Node.js backend API
- `frontend/` — Frontend app (to be added)

## Backend

```bash
cd backend
npm install
npm run dev
```

